def import_manual_results_payload(payload, allow_overwrite_results=False, dry_run=False):
    # Minimal placeholder so app can start; replace with real implementation later.
    return {
        "message": "manual_results_tool placeholder active",
        "dry_run": bool(dry_run),
        "races": 0,
        "runners": 0,
    }
