def rename_or_merge_dog(old_name, new_name, dry_run=False, preview_only=False):
    # Minimal placeholder so app can start; replace with real implementation later.
    return {
        "message": "rename_dog_tool placeholder active",
        "old_name": old_name,
        "new_name": new_name,
        "dry_run": bool(dry_run),
        "preview_only": bool(preview_only),
        "updated": 0,
    }
